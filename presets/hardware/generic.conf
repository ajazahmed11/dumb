# ==============================================================================
# DUMB Hardware Preset: Generic Desktop / Non-Tuned Laptop
# Preserves custom user fonts and local systemd user overrides.
# ==============================================================================

HARDWARE_NAME="Generic PC"

HARDWARE_PATHS=(
    "$HOME/.local/share/fonts"
    "$HOME/.config/systemd/user"
)
